# tender-agency-dashboard

## Recent Changes

- Added `isFree` attribute to tender creation (`TenderCreate` and UI form) to indicate whether a tender has no associated fee. Defaults to `false` if not specified.

